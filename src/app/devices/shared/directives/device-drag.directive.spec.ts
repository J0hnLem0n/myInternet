import { DeviceDragDirective } from './device-drag.directive';

describe('DeviceDragDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceDragDirective();
    expect(directive).toBeTruthy();
  });
});
