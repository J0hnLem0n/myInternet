import { WorkspaceDirective } from './workspace.directive';

describe('WorkspaceDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkspaceDirective();
    expect(directive).toBeTruthy();
  });
});
